<li id="micropost-<%= micropost.id %>"> <%# adding CCS id for each micropost %>
  <%link_to gravatar_for(micropost.user, size: 50), micropost.user %>
  <span class="user"><%= link_to micropost.user.name, micropost.user %></span>
  <span class="content">
    <%= micropost.content %>
    <%= image_tag micropost.display_image if micropost.image.attached? %> <%# если картинка прикреплена то хэлпер image_tag поможет нам ассоциироват картинку с микропостом в html коде %>
  <%# display_image использует метод модели micropost которая в свою очерередь использует гемы для сжатия %>
  </span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(micropost.created_at) %> ago. <%# отвечает за надписи типа Posted 15 minutes ago %>
    <% if current_user == (micropost.user) %> <%# micropost.user возвращает Юзера которому принадлежит микропост %>
    <%= link_to "delete", micropost, method: :delete, # deletemethod перекидывает нас в destroy кметод контроллера микропостов?
                data: { confirm: "You sure?" } %>
    <% end %>
  </span>
</li>